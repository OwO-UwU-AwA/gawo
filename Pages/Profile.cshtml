@page
@model ProfileModel
@{
  ViewData["Title"] = $"Gaußwoche {DateTime.Now.Year}";
}

<h2 style="font-weight: bold;" id="header" class="bs-dark">Gaußwoche @DateTime.Now.Year</h2>

<button type="button" class="btn btn-danger" onclick="location.href='/Logout'">Logout</button>